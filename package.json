{
  "name": "blackhawkbraids-checkout",
  "version": "1.0.0",
  "description": "BlackhawkBraids – secure Stripe checkout server with live bracelet pricing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --runInBand",
    "dev:static": "live-server . --port=8088 --open=/ --watch=./ --wait=80",
    "dev:static:root": "live-server . --port=8088 --open=/ --watch=./ --wait=80 --no-browser"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
